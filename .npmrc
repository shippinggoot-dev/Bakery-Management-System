# Hoist all packages to the root node_modules folder.
# This prevents TypeScript path resolution issues in Next.js monorepos
# where packages reference each other across workspaces.
shamefully-hoist=true
